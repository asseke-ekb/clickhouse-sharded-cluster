# Публикация в GitHub

Инструкция по публикации кластера ClickHouse в GitHub репозиторий.

## ✅ Подготовка завершена

Репозиторий уже инициализирован и готов к публикации:

- ✅ Git репозиторий инициализирован
- ✅ .gitignore файл создан (исключены data/ и logs/ директории)
- ✅ Все конфигурационные файлы добавлены
- ✅ Начальный коммит создан
- ✅ README.md с полной документацией

## 🚀 Шаги для публикации

### 1. Создать репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый репозиторий:

- **Repository name:** `clickhouse-sharded-cluster` (или любое другое имя)
- **Description:** ClickHouse 3-shard cluster with ZooKeeper coordination and ON CLUSTER support
- **Visibility:** Public или Private (на ваш выбор)
- **⚠️ НЕ ИНИЦИАЛИЗИРУЙТЕ с README, .gitignore или LICENSE** (они уже есть)

### 2. Подключить локальный репозиторий к GitHub

После создания репозитория GitHub покажет команды. Выполните:

```bash
cd "C:\Users\asseke\Desktop\Работа\clickhouse_shard"

# Добавить remote origin (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/clickhouse-sharded-cluster.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Запушить код
git push -u origin main
```

### 3. Проверить публикацию

После выполнения команд:

1. Обновите страницу репозитория на GitHub
2. Убедитесь, что все файлы загружены
3. Проверьте, что README.md отображается корректно

## 📋 Что включено в репозиторий

### Конфигурационные файлы

- **docker-compose-full.yml** - Локальное развертывание (Windows/macOS)
- **shard-1/**, **shard-2/**, **shard-3/** - Конфигурации для каждого шарда
  - `config/clickhouse/config.xml` - Основная конфигурация ClickHouse
  - `config/clickhouse/users.xml` - Пользователи и права доступа

### SQL скрипты

- **setup-with-cluster.sql** - Примеры ON CLUSTER команд
- **demo-tables-simple.sql** - Создание демо-таблиц (только структура)
- **insert-sample-data.sql** - Вставка тестовых данных малыми порциями
- **demo-tables.sql** - Полная версия с генерацией данных (для Linux)

### Документация

- **README.md** - Главная документация с quick start
- **PRODUCTION-DEPLOYMENT.md** - Полное руководство по деплою на 3 VM
- **DBEAVER-CONNECTION.md** - Инструкции по подключению через DBeaver
- **TROUBLESHOOTING.md** - Решение проблем (особенно для Windows)
- **DEMO-GUIDE.md** - Работа с демо-данными и примеры запросов
- **QUICK-START.md** - Быстрый старт кластера

## 🔒 Что НЕ включено (благодаря .gitignore)

- **data/** - Директории с данными ClickHouse (создаются автоматически)
- **logs/** - Лог-файлы (создаются автоматически)
- **zookeeper-*/data/** - Данные ZooKeeper
- **zookeeper-*/logs/** - Логи ZooKeeper
- Временные файлы, backup файлы

## 📝 Дополнительные команды Git

### Добавить новые изменения

```bash
cd "C:\Users\asseke\Desktop\Работа\clickhouse_shard"

# Проверить статус
git status

# Добавить измененные файлы
git add .

# Создать коммит
git commit -m "Update configuration"

# Запушить изменения
git push
```

### Клонирование репозитория

Другие пользователи смогут клонировать репозиторий:

```bash
git clone https://github.com/YOUR_USERNAME/clickhouse-sharded-cluster.git
cd clickhouse-sharded-cluster
docker-compose -f docker-compose-full.yml up -d
```

### Создать ветку для изменений

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/new-shard

# Внести изменения...

# Закоммитить
git add .
git commit -m "Add 4th shard configuration"

# Запушить ветку
git push -u origin feature/new-shard
```

## 🎯 Рекомендации

### Перед публикацией в public репозиторий

1. **Проверьте пароли** в `shard-*/config/clickhouse/users.xml`:
   - Убедитесь, что используются тестовые пароли (admin123, dbeaver123)
   - Добавьте предупреждение в README о смене паролей для production

2. **Проверьте IP адреса** в конфигах:
   - Для локального деплоя используются имена контейнеров (clickhouse-shard-01, etc.)
   - Для production deployment есть отдельный гайд с инструкциями

3. **Добавьте LICENSE файл** (опционально):
   ```bash
   # Например, MIT License
   echo "MIT License" > LICENSE
   git add LICENSE
   git commit -m "Add MIT license"
   git push
   ```

### После публикации

1. **Добавьте Topics на GitHub:**
   - Перейдите в Settings → Topics
   - Добавьте: `clickhouse`, `docker`, `sharding`, `zookeeper`, `distributed-systems`

2. **Создайте Release:**
   - Перейдите в Releases → Create a new release
   - Tag: `v1.0.0`
   - Title: `Initial Release - 3 Shard ClickHouse Cluster`
   - Description: Опишите основные возможности

3. **Обновите README.md** с корректным URL репозитория:
   - Замените `https://github.com/your-username/clickhouse-sharded-cluster.git` на актуальный URL

## 🔗 Полезные ссылки

- [GitHub: Creating a repository](https://docs.github.com/en/get-started/quickstart/create-a-repo)
- [GitHub: Pushing commits](https://docs.github.com/en/get-started/using-git/pushing-commits-to-a-remote-repository)
- [Git Basics](https://git-scm.com/book/en/v2/Getting-Started-Git-Basics)

---

**Готово!** Ваш кластер ClickHouse готов к публикации на GitHub. 🎉
